# 2026-09-21 백테스트 시작 대안 WBS

목적: ted-startup 최신 상태를 확인하고 2026-09-21부터 시작 가능한 백테스트 범위와 비의존 대안을 확정한다. 현재 판단: 전체 시장 의사결정용 실행은 차단됐고 807종목 제한 표본의 한 전략 실가격 리허설은 조건부로 가능하다. 범위: 상태 감사, 제한 표본 산정, 실행·책임 경계 문서화. 한계: REAL runner 구현과 실제 수익률 실행은 후속 작업이다.

진행률: **100%** · 완료 **5/5** · 남은 작업: 없음(계획·판정 범위) · 차단: 후속 실행은 정식 r4 미발행과 vectorbt REAL 실행 미구현의 영향을 받음.

아래 표는 의존성과 완료 기준을 관리한다. 진행률은 검증까지 완료한 항목의 가중치 합을 전체 가중치 합으로 나눈 값이며, 남은 시간의 추정값은 아니다.

| ID | 작업·산출물 | 담당 | 선행 | 완료 기준 | 가중치 | 상태 |
| --- | --- | --- | --- | --- | ---: | --- |
| W1 | ted-startup 브랜치·전달본·잡·DB 상태 감사 | data_gate 에이전트 + 메인 | 없음 | 최신 HEAD, r3 검증, r4 게이트, 예정 잡과 데이터 이상 확인 | 3 | 완료 |
| W2 | vectorbt REAL 차단·실행 경로 감사 | candidate_audit 에이전트 + 메인 | 없음 | 코드 차단과 내일 가능한 최소 범위 확인 | 2 | 완료 |
| W3 | 제한 표본 재현 집계 | 메인 | W1 | 제외 규칙과 종목·행·상태 수 기록 | 2 | 완료 |
| W4 | 시작·대안 문서 작성 | 메인 | W1~W3 | 가능/불가능 범위, 실행 순서, 책임 경계, 승격 조건 문서화 | 2 | 완료 |
| W5 | 독립 검토와 최종 교정 | candidate_audit 에이전트 + 메인 | W4 | 과대 주장·누락·코드 불일치 지적 반영 | 1 | 완료 |

## 판단과 검증 기록

- 선택·가정: “백테스트 시작”을 제한 실가격 실행 경로의 검증과 전체 시장 전략 선정으로 나누었다. 둘을 같은 완료 상태로 보고하면 현재 자료의 편향과 사건 공백이 숨겨지기 때문이다.
- 병렬 배정: ted-startup 라이브 상태와 vectorbt 최소 실행 범위를 독립 감사로 분리했다. 메인은 로컬 Parquet 표본 집계와 문서 통합을 맡았다.
- 검증: ted r3 `verify_delivery.py` 재실행은 `ok=true`, 오류·경고 0이었다. r4 readiness JSON과 원천 Parquet를 읽어 게이트를 대조했다. 제한 표본은 pandas 3.0.5·pyarrow 25.0.1로 읽기 전용 집계했다.
- 독립 검토: r3→C1 형식 불일치, REAL CLI 부재, 과거 업종 누락에 따른 `__UNKNOWN__` 25% 한도, 전체 규모 메모리 위험을 반영했다. 제한 표본은 모든 기록된 기업행사·계수 변동까지 제외하는 807종목으로 다시 계산했고, 첫 실행을 한 전략·한 연도로 축소했다.
- 범위 변경: 없음.
- 승인: 로컬 문서 작성만 요청 범위에 포함되며 푸시하지 않는다.
