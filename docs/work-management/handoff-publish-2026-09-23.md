# 006740 증거·기존 연구 변경 통합 보존 WBS

목적: 006740 직접 증거와 입력 연결 점검을 인계하고, 사용자가 함께 푸시하도록 요청한 기존 vectorbt 변경을 검증해 보존한다. 코드·자료 검증과 인계 작성을 완료했고 공개 커밋을 준비했다. 공급자 실행 중 세션의 작업은 별도 브랜치로 분리하며 DB·정책·봉인 입력·실제 백테스트는 변경하지 않는다.

진행률: **60% · 3/5 완료**. 남은 작업: 인계 커밋 마무리와 푸시 검증. 푸시는 사용자가 명시적으로 승인했다.

| ID | 산출물 | 담당 | 선행 | 완료 기준 | 가중치 | 상태 |
| --- | --- | --- | --- | --- | ---: | --- |
| P1 | 세션·변경 범위 실측 | 메인 | 없음 | 실행 상태·Git·파일 규모 확인 | 1 | 완료 |
| P2 | 기존 코드·감사·증거 검증 | 메인 + 독립 검토자 | P1 | 관련 테스트·해시·민감정보 검사 | 1 | 완료 |
| P3 | 연결 감사·재개 문서 | 메인 | P1 | 과거 기록과 최신 판단 구분 | 1 | 완료 |
| P4 | 목적별 커밋 | 메인 | P2,P3 | staged 범위·공백·원문 바이트 검사 | 1 | 대기 |
| P5 | 원격 푸시·종료 확인 | 메인 | P4 | 원격 SHA 일치·잔여 변경 설명 | 1 | 대기 |

## 범위와 실행 상태

- vectorbt 시작: `research/ohlcv20-handoff-20260922`, HEAD `ceae86d`; 원격 같은 브랜치 `fb982ae`. 기존 미푸시 승인계수 어댑터 커밋도 함께 전달한다.
- 기존 변경: 확장 전략·테스트·보고서, 계수 v2 인수 감사·자료, live-quality 관측 보고, PRD, Codex 운영 설정·지침. 최초 미추적 91파일 약 17.9MB이며 가장 큰 파일은 전략 JSON 약 10.2MB다.
- 2026-09-23 Orca 실측: vectorbt의 실행 에이전트는 현재 Codex 하나다. ted-startup은 별도 Claude가 체결 승인 설계 `386b553` 푸시 후 CI 확인 중이었다. 이전에 기존 변경 전체를 다른 세션 작업이라고 표현한 것은 출처를 검증하지 않은 표현이었다.
- 세션 이력 검색은 비활성 상태다. 각 미커밋 파일의 작성 세션은 확정하지 않으며 WBS·코드·테스트를 기준으로 검토한다.
- supplier 증거는 `withwooyong/evidence-006740-handoff-20260923` 별도 브랜치에 보존한다. 원래 supplier 작업 디렉터리의 브랜치·index·상태 문서는 수정하지 않는다.
- 원문 묶음은 기존 29파일을 바이트 그대로 복사하며 기존 manifest·receipt를 재작성하지 않는다. 과거 WBS의 커밋 금지 문구는 확보 당시 범위이며, 현재 사용자 요청으로 이번 보존·푸시만 승인됐다.

## 다음 작업과 제약

006740 정정 근거가 선택되는 공급자 입력 revision을 설계하는 것이 다음 작업이다. 정정 당시 이용 가능한 근거와 7월 사후 완료 증거를 분리하고, 계수·거래량 단위 정책은 자동 승격하지 않는다. 이번 커밋·푸시는 입력 연결 구현 또는 실행 승인을 뜻하지 않는다.

재개: `C:/Users/aeby/vscode/stock/vectorbt`에서 Git 상태와 HANDOFF, 이 WBS를 읽는다. 시작 문장: “006740 직접 증거 인계를 읽고, 공급자 입력 revision의 출처 연결과 known-at 분리안을 작성해줘. DB·봉인 입력·정책·백테스트는 변경하지 마.”

## 마무리 검증

- 확장 전략 35개와 기존 v3 실행 회귀 36개, OHLCV 입력·preflight 15개, 계수 인수 감사 9개: 서로 다른 관련 테스트 **95개 통과**. 실제 시장 백테스트를 다시 실행하지 않았다.
- 확장 결과 2,598행(성공 1,842·차단 756), 상세 통계·성과·summary 3개·실행 모델 해시 일치. 원천 DB 재조회와 과거 전체 회귀 560개 재실행은 하지 않았다.
- 공급자 증거 검증기: 내부 28파일+외부 6참조 및 다섯 등식 PASS. 원문은 공개 DART/KIND 자료이며 외부 참조 원자료는 이 푸시에 포함되지 않는다.
- 기존 자료 96파일 자격증명 패턴 검사에서 발견 없음. 공개 자료에 사용자 로컬 경로·DB 이름은 남으며 자격증명은 아니다.
- 독립 검토에서 줄바꿈 변환 위험을 수정했다. `.gitattributes`로 해시 대상 자료와 실행 코드 바이트를 보존하고, 공식 원문·자동 생성 자료의 공백은 보존했다. 일반 코드·문서의 공백 검사는 유지한다.
- 봉인된 계수 인수 README는 원본 SHA `97ad1be74f2cbeb7a25b8918f1a4bb689f03ca5475e9d080e7db5376a2c4eabc`를 유지한다. 최신 판단은 루트 HANDOFF와 별도 006740 인계에만 기록한다.
- 공개 작업에서 새 수집·정책 변경·실제 신호 및 백테스트 실행·DB 접근은 하지 않았다.
