# 저장소 작업 지침

이 문서는 vectorbt 저장소에서 작업하는 에이전트가 구조, 개발 명령, 검증 기준을 빠르게 파악하기 위한 지침이다. 핵심 원칙은 기존 pandas API와 수치 계산 동작을 유지하면서 요청한 범위만 수정하는 것이다. 저장소 전체에 적용하며, 의존성 및 CI 명령이 변경되면 `pyproject.toml`과 `.github/workflows/`의 실제 설정을 우선 확인한다.

## 작업 운영 규칙: 빠른 병렬 실행과 검증된 완료

모든 요청은 품질을 유지하면서 가능한 한 빠르게 완료한다. 메인 세션은 작업 분해, 병렬 위임, 결과 통합, 검증과 다음 단계 진행을 책임진다. 이 규칙은 이 저장소의 모든 작업에 적용하며, 도구·모델의 실제 가용성과 상위 실행 제약을 따른다.

### WBS로 범위와 남은 작업을 관리한다

- 작업 시작 시 WBS(Work Breakdown Structure, 작업 분해 구조)를 만들고, 각 항목에 산출물, 담당, 선행 의존성, 완료 기준, 가중치, 상태를 기록한다. 작은 요청은 짧은 표로 관리하고, 여러 단계 또는 세션에 걸치는 작업은 파일로 남긴다.
- 기존 작업 WBS가 있으면 재사용한다. 파일로 관리하는 새 작업은 `docs/work-management/wbs-template.md`를 복사하여 작업별 파일로 관리한다. 기존 연구 WBS와 무관한 작업은 섞지 않는다.
- 상태는 `대기`, `진행`, `검증 중`, `차단`, `완료`를 사용한다. 구현만 끝난 항목은 검증 전까지 완료로 세지 않는다.
- 진행률은 `완료 항목 가중치 합 / 전체 가중치 합 × 100`으로 계산한다. 가중치는 착수 시 예상 작업량을 기준으로 정하고, 범위 변경 시 변경 이유와 함께 갱신한다. 진행률은 남은 시간의 보장이 아니다.
- 주요 진행 보고와 최종 보고에 진행률, 완료/전체 항목 수, 남은 작업과 차단 요인을 표시한다. 승인·푸시가 요청 범위에 포함되면 별도 항목으로 유지하고, 푸시 전 준비 완료와 전체 완료를 구분한다.

### 독립 작업은 병렬 위임하고 메인 세션이 통합한다

- 동시에 진행해도 유효한 독립 작업은 모두 병렬화한다. 조사, 구현, 문서, 테스트, 독립 검토를 의존성과 파일 소유 범위에 따라 나눈다. 선행 결과가 필요한 작업은 순차 실행하고 병렬 슬롯이 부족하면 준비된 작업부터 배정한다.
- 각 에이전트에 목적, 입력, 수정 가능한 파일, 산출물, 완료 기준과 검증 명령을 명시한다. 동일 파일에 대한 동시 수정을 피하고 공유 파일의 통합 담당을 정한다.
- 실제 사용 가능한 모델 중 작업 난도와 오류 비용에 맞게 선택한다. 단순 탐색·정형 수정은 빠른 모델, 일반 구현·테스트는 코딩 모델, 설계·수치 정확성·복잡한 디버깅·최종 판단은 추론 역량이 높은 모델에 맡긴다. 모델 선택이 불가능하면 사용 가능한 기본 모델로 진행하고 제한을 알린다.
- 메인 세션도 통합 준비나 독립 작업을 수행하며, 에이전트 결과의 근거·diff·검증 결과를 확인하고 충돌을 해결한 뒤 다음 단계를 진행한다. 위임 결과를 검증 없이 완료로 선언하지 않는다.
- 독립 검토가 가능한 산출물은 작성자와 다른 에이전트가 검토한다. 병렬화할 독립 작업이 없는 짧은 요청은 직접 처리하고 이유를 간단히 알린다.

### Astra 팀장과 작업별 팀원 모델을 사용한다

팀장은 요구사항, 작업 분해, 의존성, 통합과 최종 판단을 책임진다. 실행 기본값은 `.codex/config.toml`에 두고, 팀원 생성 시 아래 기준에 따라 모델과 추론 강도(effort)를 명시한다. 이 표는 프로젝트의 시작 기준이며 실제 작업 결과와 사용 가능한 모델을 바탕으로 조정한다.

| 역할·작업 | 모델 | 기본 effort |
| --- | --- | --- |
| 팀장: 설계, WBS, 통합, 최종 판단 | `gpt-6-astra` | `high` |
| 복잡한 구현·디버깅, pandas/Numba/Rust 수치 동등성 | `gpt-5.6-sol` | `high` |
| 범위가 명확한 일반 구현·테스트·문서 | `gpt-5.6-terra` | `medium` |
| 파일·호출부 탐색, 정형 점검, 자료 요약 | `gpt-5.6-luna` | 단순 작업 `low`, 해석 필요 시 `medium` |
| 작성자와 다른 팀원의 독립 검토 | `gpt-5.6-sol` | `high` |
| 오류 비용이 큰 수치·방법론의 독립 검토 | `gpt-6-astra` | `high` |

- 역할이 지정되지 않은 팀원 기본값은 Sol `medium`이다. 난도가 높은 설계·수치 오류는 필요할 때 `xhigh`로 높이고 이유를 WBS에 남긴다. 모든 작업에 최고 effort를 일괄 적용하지 않는다.
- 팀장 1명과 동시 팀원 최대 3명으로 운영한다. 하위 팀원의 추가 위임도 전체 가용 슬롯 안에서 팀장이 조정한다. 실행 환경의 한도가 더 낮으면 그 한도를 따른다.
- 팀원 생성 전 실제 모델·effort 지원 여부를 확인한다. 지원하지 않으면 가용 모델 중 적합한 대안을 선택하고 보고한다. 모델 이름을 임의로 바꾸거나 적용 여부를 추정하지 않는다.
- 전체 이력 복제가 부모 모델을 강제 상속하는 도구에서는, 모델을 다르게 지정할 때 전체 복제를 사용하지 않는다. 현재 `collaboration.spawn_agent`에서는 `fork_turns="none"` 또는 필요한 최근 턴 수를 지정하고 `model`, `reasoning_effort`를 명시한다. 다른 도구에서는 해당 도구의 계약을 따른다.
- 짧은 단일 작업이나 선행 결과에 강하게 의존하는 작업은 직접 처리한다. 병렬화로 얻는 이득보다 설명·충돌 해결 비용이 크면 위임하지 않고 이유를 짧게 남긴다.

### 팀장 컨텍스트는 결정과 검증 근거 중심으로 관리한다

상세 탐색·구현·로그 분석은 팀원에게 분배하고, 팀장은 요구사항·인터페이스·결정·위험·검증 결과를 유지한다. 대화가 압축되거나 새 세션으로 넘어가도 파일에서 작업을 재개할 수 있게 한다.

- 작업 지시에는 `목적 / 입력·관련 경로 / 수정 허용 파일 / 의존성·인터페이스 / 산출물 / 완료 기준·검증 명령 / 금지사항`을 포함한다. 전체 대화 대신 해당 작업에 필요한 정보만 전달한다.
- 팀원 보고는 `결론 / 변경 파일 / 실제 검증 명령·결과 / 미확정 사항·위험 / 다음 단계`로 간결하게 작성한다. 긴 로그·원자료는 파일로 남기고 경로와 필요한 부분만 보고한다. 메인은 결론을 뒷받침하는 diff와 증거를 직접 확인한다.
- 같은 파일의 동시 수정은 피하고, 공통 인터페이스·공유 파일·WBS의 통합 담당은 메인으로 둔다. 작업 범위가 바뀌면 관련 팀원에게 변경된 계약을 전달한다.
- 작업별 WBS를 진행 상태의 기준으로 사용한다. 단계 완료·중요한 결정·세션 전환 시 결정 이유, 검증 근거, 남은 작업과 재개 조건을 갱신한다. 인계가 필요하면 기존 인계 문서를 확인하여 요청 범위의 내용만 갱신하고 중복 문서를 늘리지 않는다.
- 자동 압축 임계값과 컨텍스트 창 크기는 기본값을 유지한다. 설정 파일 수정만으로 실행 중인 세션이 바뀌었다고 보고하지 않는다. 새 세션의 실제 모델·effort와 호스트 오버라이드를 확인한다.

### 검증된 작업 경계에서 clear와 다음 작업을 안내한다

요청한 작업이 검증·검토까지 끝나고 파일만으로 재개할 수 있으면, 최종 보고에서 사용자에게 clear 후 새 대화를 시작하도록 권한다. 긴 대화의 불필요한 입력을 줄이는 것이 목적이며 토큰·비용 절감이나 품질 향상을 보장하지 않는다. 짧은 후속 질문마다 초기화를 권하거나 승인된 진행 중 작업을 임의로 끊지 않는다.

- 먼저 현재 작업의 팀원·테스트·백그라운드 작업이 종료됐는지 확인한다. 감시·후속 응답·미수집 결과가 필요하면 clear를 보류하고 이유를 보고한다. 다른 세션의 작업은 종료하지 않는다.
- 현재 작업 WBS 또는 기존 인계 문서의 해당 절에 `작업 디렉터리·브랜치 / 완료 범위 / 핵심 결정·제약 / 변경 파일·미커밋 상태 / 검증 근거·미검증 사항 / 승인·차단 / 다음 작업·완료 기준`을 간결하게 저장한다. 문서 링크로 상세를 연결하고, 관련 없는 인계나 병행 작업을 덮어쓰지 않는다.
- 파일에 기록되지 않은 중요 결정이나 실패한 필수 검증이 남아 있으면 완료·clear 가능으로 보고하지 않는다. 전환을 위해 커밋·푸시를 추가하거나 임시 파일·대화 이력을 삭제하지 않는다. 로컬 산출물만 요청된 경우 미커밋 상태를 기록한 것으로 충분하다.
- 최종 보고 말미에는 `clear 권고 또는 보류 이유 / 추천 다음 작업 1개와 이유 / 새 대화용 시작 문장`을 제공한다. 다음 작업은 최신 WBS와 의존성에서 고르며 추천을 새 작업의 실행 승인으로 간주하지 않는다.
- Codex CLI의 `/clear` 또는 `/new`는 새 대화를 시작한다. PowerShell `clear`·`Clear-Host`나 화면 지우기는 컨텍스트 초기화가 아니다. 호스트에서 지원하지 않으면 새 대화 UI를 안내하고, 직접 실행하거나 현재 대화가 초기화됐다고 주장하지 않는다.
- 새 세션은 안내한 작업 디렉터리에서 `git status --short`와 현재 브랜치를 확인하고 프로젝트 지침·작업 WBS·인계의 관련 부분을 읽어 재개한다. 이전 검증은 변경·오래된 증거·미해결 위험이 있을 때만 필요한 범위로 재실행한다. 자동 압축 설정은 유지한다.

### 추천 가능한 판단은 진행하고 승인은 구체적인 결과로 요청한다

- HITL(Human-in-the-loop, 사람의 판단·승인)은 필요한 지점에 집중한다. 요청 범위 안에서 AI가 합리적으로 추천할 수 있는 가역적 선택은 근거와 가정을 기록하고 진행한다.
- 구현, 관련 검증, 독립 검토와 필요한 수정을 마쳐 푸시 가능한 결과를 준비한 다음 사용자에게 변경 요약, 검증 결과, 남은 한계와 함께 푸시 승인을 요청한다. 이미 명시적으로 승인된 행동은 다시 묻지 않는다.
- 사용자 결정 없이는 범위를 확정할 수 없거나, 복구하기 어려운 작업·필수 권한·외부 전송에 사전 승인이 필요한 경우에는 해당 행동 전에 묻고 독립 작업은 계속한다. 무응답은 승인으로 취급하지 않는다.
- 문서 작성 등 로컬 산출물만 요청된 작업은 그 산출물의 검증으로 완료하며, 불필요한 푸시 승인 절차를 추가하지 않는다.

### 반복 절차는 재사용 가능한 자동화로 남긴다

- 반복 실행이 확인되거나 예정된 절차는 기존 도구를 우선 재사용하고, 적합한 script·skill·hook·템플릿으로 만든다. 결정적인 검사·집계는 script, 판단을 포함한 작업 절차는 skill, 명확한 이벤트에 연결된 검사는 hook을 고려한다.
- 자동화에는 실행 방법, 입력·출력, 실패 처리와 검증 방법을 남긴다. 재실행 시 사용자 변경을 훼손하지 않도록 하고, 숨은 외부 전송이나 푸시를 포함하지 않는다.
- 일회성 작업을 위한 불필요한 프레임워크를 만들지 않는다. 자동화 범위도 WBS에 포함하고, 도구 생성 비용과 반복 절감 효과를 고려한다.

## 프로젝트 구조

vectorbt는 pandas·NumPy 배열로 여러 매매 전략을 동시에 백테스트하는 Python 라이브러리다. Numba의 JIT(실행 시점 컴파일) 커널과 선택적 Rust 엔진으로 계산을 가속한다. Python 지원 범위는 현재 3.11~3.14다.

| 경로 | 역할 |
| --- | --- |
| `vectorbt/base/` | 배열 브로드캐스팅, 인덱싱, pandas 메타데이터를 보존하는 `ArrayWrapper` |
| `vectorbt/generic/` | 롤링 계산 등 공통 시계열 연산과 액세서 |
| `vectorbt/indicators/`, `signals/`, `labels/`, `returns/` | 지표, 매매 신호, 학습용 레이블, 수익률 분석 |
| `vectorbt/portfolio/`, `records/` | 포트폴리오 시뮬레이션과 주문·거래 등 구조화 레코드 |
| `vectorbt/data/`, `utils/`, `messaging/` | 데이터 소스 연동, 공통 유틸리티, 메시징 |
| `vectorbt/_engine.py`, `vectorbt/_settings.py` | 엔진 선택과 전역 설정 |
| `rust/src/` | PyO3를 사용하는 선택적 확장 패키지 `vectorbt-rust` |
| `tests/` | 기능별 pytest 테스트와 엔진 비교 테스트 |
| `docs/`, `examples/`, `apps/`, `benchmarks/` | MkDocs 문서, 예제, 샘플 앱, 성능 측정 |

표에서 한 셀에 나열한 축약 디렉터리는 모두 `vectorbt/` 아래에 있다.

## 설치와 테스트

기본 명령은 저장소 루트에서 실행한다. CI는 `uv`를 사용하며 Linux·Windows·macOS와 Python 3.11~3.14 조합에서 Python 전용 환경과 Rust 설치 환경을 각각 검사한다.

```powershell
# 최초 환경 구성: Python 버전은 지원 범위 안에서 선택
uv python install 3.11
uv venv --python 3.11
uv pip install --python .venv -e ".[test]"

# 변경한 기능부터 검사한 뒤 필요한 범위로 확대
uv run --python .venv --no-sync pytest tests/test_generic.py
uv run --python .venv --no-sync pytest tests/test_engine.py
uv run --python .venv --no-sync pytest tests/
```

`uv`를 사용하지 않는 경우 활성화된 가상환경에서 `python -m pip install -e ".[test]"`와 `python -m pytest tests/`를 사용한다. 기존 개발환경이 있으면 먼저 확인하고 재사용한다.

Rust 변경은 Rust 툴체인을 준비하고 로컬 확장을 다시 빌드하여 검증한다.

```powershell
uv pip install --python .venv -e ".[test-rust]"
uv run --python .venv --no-sync python -m maturin develop --manifest-path rust/Cargo.toml --release
cargo test --manifest-path rust/Cargo.toml
uv run --python .venv --no-sync pytest tests/test_engine.py
uv run --python .venv --no-sync pytest tests/
```

Rust가 없거나 버전이 호환되지 않으면 관련 테스트가 건너뛰어질 수 있다. 테스트 통과와 Rust 경로의 실제 실행을 구분하여 보고한다. 성능 측정은 release 빌드를 사용하며 절차는 `rust/README.md`와 `benchmarks/README.md`를 따른다.

## 구현 및 검증 원칙

- 주변 코드의 명명, 타입 힌트, 독스트링, import 방식을 따른다. Python의 Black 설정은 줄 길이 120이며, 이를 이유로 무관한 파일 전체를 재정렬하지 않는다.
- `nb.py`는 NumPy 배열과 Numba 호환 타입을 처리하는 참조 커널이다. Rust 분기는 `dispatch.py`와 `_engine.py`의 기존 패턴을 사용하고 Numba 커널에 Rust 의존성을 넣지 않는다.
- 사용자 API를 수정할 때 브로드캐스팅, 반환 차원, index·columns·freq, 그룹화 동작을 보존한다. 일반적으로 행은 시간, 열은 자산 또는 파라미터 조합이며, `_1d` 함수 등 예외는 해당 함수의 계약을 따른다.
- `engine="auto"`는 지원되는 호출에서 호환 Rust 엔진을 선택하고 나머지는 Numba로 돌아간다. `engine="rust"`를 명시한 경우 지원 불가 사유를 오류로 전달하는 계약을 유지한다.
- 난수 연산의 `auto`는 기존 난수 스트림 보존을 위해 Numba를 유지한다. 콜백과 지원하지 않는 입력의 대체 실행 경로도 확인한다.
- Rust 커널 추가 시 Numba와 인자 순서·반환 형태를 맞추고 PyO3 등록, Python 디스패치, 결과 일치·대체 실행·명시적 오류 테스트를 함께 반영한다. 구조화 dtype 변경은 Python과 Rust의 필드 및 메모리 배치를 함께 확인한다.
- 계산 변경은 재현 가능한 회귀 사례로 검증한다. 관련된 NaN, 빈 배열, 경계 파라미터, dtype, C/F 메모리 배치, 수치 안정성을 검사하며 허용 오차를 무작정 늘려 실패를 숨기지 않는다.
- 전역 설정이나 엔진 캐시를 변경한 테스트는 기존 정리 패턴인 `vbt.settings.reset()`과 `_engine.clear_engine_cache()`를 참고하여 상태를 복원한다.
- `tests/conftest.py`는 pandas 비교 함수의 dtype·시간 단위를 정규화하고 Rust 테스트의 skip을 처리한다. 비교 실패를 조사할 때 이 동작을 고려한다.
- 동작 변경에는 관련 테스트를 추가하거나 수정한다. 문서만 바꾸는 경우 불필요한 전체 테스트 대신 내용, 경로, 명령의 정합성을 확인한다. 완료 보고에는 실제 실행한 검증과 미실행 항목을 구분한다.

## 문서 작성과 빌드

사용자 설명과 새 작업 지침은 기본적으로 한글로 작성한다. 기존 코드 식별자와 문서의 언어·템플릿은 요청 없이 일괄 번역하지 않는다.

Markdown을 새로 작성하거나 의미 있게 수정할 때는 목적·핵심 결론 또는 현재 판단·범위와 한계를 첫머리에 짧게 제시한다. 본문은 핵심 요약 → 전체 구조와 판단 기준 → 근거와 비교 → 세부 사례 → 방법론과 출처 순서로 구성한다. 긴 문서는 요약 다음에 각 절의 역할과 읽기 순서를 안내한다.

각 절과 문단은 핵심 의미부터 설명하고, 큰 표 앞에는 비교 대상과 읽어낼 점을 쓴다. 낯선 용어는 처음에 풀어 쓰고 사실·추정·권장안·미확정 사항을 구분한다. 짧은 문서에 불필요한 목차를 추가하지 않으며 기존 메타데이터와 기계 판독 형식을 보존한다. 요청과 무관한 문서는 재작성하지 않는다.

사이트 문서 또는 API 문서를 변경한 경우 다음은 CI 기준 빌드 절차다. API 생성 결과를 직접 고치기 전에 원본 독스트링과 생성 스크립트를 확인한다.

```powershell
# 저장소 루트에서 의존성 설치
uv pip install --python .venv -e ".[full,docs]"
Push-Location docs
try {
    uv run --python ../.venv --no-sync python generate_api.py
    uv run --python ../.venv --no-sync python update_api_nav.py
    uv run --python ../.venv --no-sync mkdocs build --strict
} finally {
    Pop-Location
}
```

## 변경 범위와 참고 자료

작업 전후 `git status --short`와 diff로 변경 범위를 확인한다. 사용자의 기존 수정과 추적되지 않은 파일을 보존하고, 의존성·생성물·버전·릴리스 설정은 작업에 필요한 경우에만 변경한다. 커밋과 PR은 하나의 관심사로 유지하고 변경 이유 및 검증 결과를 설명한다.

구체적인 근거는 `pyproject.toml`, `.github/workflows/tests.yml`, `.github/workflows/docs.yml`, `rust/README.md`, `docs/docs/getting-started/contributing.md`에서 확인한다. 라이선스 관련 변경은 루트 `LICENSE.md`와 별도 문서 라이선스인 `docs/LICENSE.md`를 각각 확인한다.
